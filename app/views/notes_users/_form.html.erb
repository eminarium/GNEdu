<%= form_for(@notes_user) do |f| %>
  <% if @notes_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notes_user.errors.count, "error") %> prohibited this notes_user from being saved:</h2>

      <ul>
      <% @notes_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :note_id %><br>
    <%= f.text_field :note_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
