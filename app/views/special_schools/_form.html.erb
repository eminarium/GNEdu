<%= form_for(@special_school) do |f| %>
  <% if @special_school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@special_school.errors.count, "error") %> prohibited this special_school from being saved:</h2>

      <ul>
      <% @special_school.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div><label class="col-sm-4 control-label">Ýörite-Orta Okuw Jaýynyň Ady</label>
  <div class="col-sm-8"><%= f.text_field :specialSchoolName, :type => "text", :class => "form-control" %></div>
</div>

<br><br><br>

<div><label class="col-sm-4 control-label">Bellikler</label>
  <div class="col-sm-8"><%= f.text_area :notes, :type => "text", :class => "form-control"  %></div>
</div>

<br><br><br>

<div class="actions">
  <%= f.submit "Kabul Et", :class => "btn btn-success" %>
</div>

<% end %>