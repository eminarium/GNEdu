<%= form_for(@special_group) do |f| %>
    <% if @special_group.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@special_group.errors.count, "error") %> prohibited this special_school from being saved:</h2>

          <ul>
            <% @special_group.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div><label class="col-sm-4 control-label">Ýörite Toparyň Ady</label>
      <div class="col-sm-8"><%= f.text_field :specialGroupTitle, :type => "text", :class => "form-control" %></div>
    </div>

    <br><br><br>

    <div><label class="col-sm-4 control-label">Tapgyry</label>
      <div class="col-sm-8">
        <%= f.collection_select(:season_id, @seasons, :id, :seasonTitle, :include_blank => t("Please_select"), :class => "form-control" ) %>
      </div>
    </div>

    <br><br><br>

    <div><label class="col-sm-4 control-label">Bellikler</label>
      <div class="col-sm-8"><%= f.text_area :notes, :type => "text", :class => "form-control"  %></div>
    </div>

    <br><br><br>

    <div class="actions">
      <%= f.submit "Kabul Et", :class => "btn btn-success" %>
    </div>

<% end %>