<%= form_for(@role) do |f| %>
  <% if @role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
      <% @role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<% @i=0 %>

<div><label class="col-sm-4 control-label">Role Ady</label>
  <div class="col-sm-8"><%= f.text_field :name, :type => "text", :class => "form-control"  %></div>
</div>

<br><br><br>

<div><label class="col-sm-4 control-label">Işlemeli Tapgyry</label>
  <div class="col-sm-8"><%= f.collection_select(:season_id, @seasons, :id, :seasonNo, :include_blank => t("Please_select"), :class => "form-control" ) %> </div>
</div>

<br><br><br>

    <div><label class="col-sm-4 control-label">Bellikler</label>
  <div class="col-sm-8"><%= f.text_area :notes, :type => "text", :class => "form-control"  %></div>
</div>

<br><br><br><br>

<div class="actions">
  <%= f.submit "Kabul Et", :class => "btn btn-success" %>
</div>

<% end %>